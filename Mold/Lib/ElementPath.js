Seed({		name : "Mold.Lib.ElementPath",		dna : "class",		author : "Jan Kaufmann",		include : [			"Mold.Lib.Element"		]	},	function(startElement, targetElement, searchDirection){				var _list = [];				var _searchRevers = function(start, target){			_list.push(start);			if(start !== target){				start = start.parentNode;				_searchRevers(start, target);			}		}				if(searchDirection === "reverse"){			_searchRevers(startElement, targetElement);		}				return {			getElementList : function(){				return _list;			},			getFiltertList : function(className){				var output = []				for(var i = 0; i < _list.length; i++){					var element = new Mold.Lib.Element(_list[i]);					if(element.hasClass(className)){						output.push(_list[i]);					}				}				return output;			}		}	});